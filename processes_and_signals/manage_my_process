#!/usr/bin/env bash
# Indefinitely writes "I am alive!" to /tmp/my_process every 2 seconds

# Exit promptly on SIGTERM/SIGINT
trap 'exit 0' TERM INT

# Ensure the output file exists
: > /tmp/my_process 2>/dev/null || true

while true; do
  echo "I am alive!" >> /tmp/my_process
  sleep 2
done
