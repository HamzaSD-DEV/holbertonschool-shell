#!/usr/bin/env bash
# Stop the 4-to_infinity_and_beyond process using kill

# Find PIDs of the target script (match full command line)
pgrep -f "4-to_infinity_and_beyond" | while read -r pid; do
  # Send SIGTERM (default) to stop each matching process
  kill "$pid" 2>/dev/null || true
done
